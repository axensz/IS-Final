<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IronInsight"
        tools:targetApi="31" >
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".rutinas"/>

        <activity
            android:name=".cambiar_contrasena"/>

        <activity
            android:name=".crear_rutina"/>

        <activity
            android:name=".ejercicios"/>

        <activity
            android:name=".home"/>

        <activity
            android:name=".medidas_corporales"/>

        <activity
            android:name=".perfil"/>

        <activity
            android:name=".recuperar_contrasena"/>

        <activity
            android:name=".registrarse"/>

        <activity
            android:name=".ver_progreso"/>

        <activity
            android:name=".iniciar_sesion"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>